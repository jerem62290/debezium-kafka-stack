---
- name: Recharger systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Redémarrer akhq
  ansible.builtin.systemd:
    name: akhq
    state: restarted
    daemon_reload: true

- name: Recharger firewalld
  ansible.builtin.command: firewall-cmd --reload
  changed_when: true

