# Identité propre au worker, même clé de FileConfigProvider sur tous les workers.
kafka.sasl.jaas.config=org.apache.kafka.common.security.plain.PlainLoginModule required username="{{ inventory_hostname }}" password="{{ hostvars['localhost'].kafka_sasl_passwords[inventory_hostname] }}";
