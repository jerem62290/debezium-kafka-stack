---
route:
  receiver: local-only
  group_by: [alertname, cluster]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

# Aucun envoi externe n'est activé en environnement air-gap. Ajoutez ici votre
# relais SMTP, webhook interne ou outil d'astreinte.
receivers:
  - name: local-only

