Déposez les artefacts dans les sous-répertoires prévus, puis lancez :

```bash
./scripts/build-sha512sums.sh
./scripts/verify-airgap-bundle.sh
```

Le fichier `SHA512SUMS` est volontairement absent de l'archive initiale.

